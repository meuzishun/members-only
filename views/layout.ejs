<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Members-Only Message Board</title>
    <link rel="stylesheet" href="stylesheets/style.css" />
    <script src="javascripts/sign-up-validation.js" defer></script>
  </head>
  <body>
    <header class="header">
      <div class="welcome">
        <h1>Welcome to the <span>Members-Only Message Board</span></h1>
      </div>
      <% if (user) { %>
      <div class="greeting">
        <p>Hi there, <span class="user-name"><%= user.firstName %></span></p>
      </div>
      <% } else { %>
      <div></div>
      <% } %>
      <nav class="navigation">
        <ul>
          <li><a href="/">home</a></li>
          <% if (user) { %>
          <li><a href="/new-message">new message</a></li>
          <li><a href="/log-out">log out</a></li>
          <% } else { %>
          <li><a href="/sign-up">sign up</a></li>
          <li><a href="/sign-in">sign in</a></li>
          <% } %>
        </ul>
      </nav>
    </header>
    <main class="page-content"><%- body %></main>
  </body>
</html>
